# FastAPI and ASGI
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
supabase==2.0.2
asyncpg==0.29.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-decouple==3.8

# External APIs
httpx>=0.24.0,<0.25.0
aiohttp==3.9.1
googlemaps==4.10.0

# Image Processing
Pillow==12.3.0
opencv-python==4.14.0.94
numpy>=2.3.0,<3.0.0

# Local ML — clothing category/color detection (patrickjohncyh/fashion-clip)
# and background removal (rembg/u2net) as a pre-classification step, both
# fully on-device after the one-time model download. CPU build; install
# torch with --index-url https://download.pytorch.org/whl/cpu for a smaller
# download. rembg requires numpy>=2.3, which is why opencv-python/Pillow are
# pinned to numpy-2-compatible builds above.
torch==2.13.0
transformers==5.15.0
rembg==2.0.81
onnxruntime==1.29.0

# Caching & Rate Limiting
redis==5.0.1
aioredis==2.0.1

# Utilities
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.3.0
python-dotenv==1.0.0
loguru==0.7.2

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
isort==5.12.0
